# MatchUp Frontend (Prototype)

This folder contains a minimal Vite + React + TypeScript prototype implementing the web frontend outlined in WEB_FRONTEND_SPEC.md.

Quick start:

```bash
cd matchup-frontend
npm install
npm run dev
```

The project includes Tailwind CSS and a small mock API used by the initial Matches tab.
